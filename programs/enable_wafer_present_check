;************************************************************************************************
; 1. 함수 이름 : enable_wafer_present_check(p_i_enable)
;
; 2. 함수 설명 : EXTEND 동작 시 웨이퍼 유/무를 판단하기 위한 기능을 켜고 끄는 FLAG 역할을 하는 함수입니다.
;************************************************************************************************
;
.PARAM p_i_enable
;
;
        g_enable_wafer_present_check = p_i_enable
        Call set_wafer_presence_mismatch(FALSE)