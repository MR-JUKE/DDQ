;************************************************************************************************
; 1. 함수 이름 : enable_gate_open_check(p_i_is_enable)
;
; 2. 함수 설명 : EXTEND 동작 시 스테이션 게이트 계폐 여부를 판단하고 동작을 일시 정지하는 기능을 켜고 끄는 FLAG 역할을 하는 함수입니다.
;************************************************************************************************
;
.PARAM p_i_is_enable
;
;
        g_enable_gate_open_check = p_i_is_enable
        Call set_gate_open_error(FALSE)