;************************************************************************************************
; 1. 함수 이름 : b_get_gate_open_check_enable(p_o_is_enable)
;
; 2. 함수 설명 : EXTEND 동작 시 스테이션 게이트 계폐 여부를 판단하고 동작을 일시 정지하는 기능을 켰는지, 껐는지 FLAG 변수 값을 가져오는 Sub task 함수입니다.
;************************************************************************************************
;
.PARAM p_o_is_enable
;
;
        p_o_is_enable = g_enable_gate_open_check